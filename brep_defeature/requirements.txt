# ML 管线（任意平台均可，pip 安装）
torch>=2.0
torch_geometric>=2.4
numpy>=1.24
pandas>=2.0

# 测试
pytest>=7.0

# 几何后端（仅 occ 子包需要；conda 安装，pip 无法安装）
#   conda install -c conda-forge pythonocc-core>=7.7
# pythonocc-core
