# 2026-07-24

## 今日完成

- 为 4-20、IL-78、KC-46、95-737 补充并确认原生贴花标注。
- 95-737 的原生窗户改用内环拓扑自动筛选：排除舱门等大型异形开口后，82 个候选均已人工确认。
- 95-737 已写入 82 个 `window` 实例、428 个窗户面；原有铆钉和贴花标注保持不变。
- 清理了已淘汰的固定模板候选和聚类脚本，只保留当前窗户候选流程。

## 当前规则

- 先自动导出窗户候选并整体可视化，通过后才写入 `.labels.json`。
- 只做原生特征标注，暂不进行注入、汽车筛选或训练集划分。

## 窗户识别方法

1. 以已确认窗户为参考，遍历模型中带内环（`inner loop`）的面。
2. 当前有效的客舱窗特征为：内环由 4 条边组成，内环周围有 4 个小相邻面，且相邻面面积相对宿主面较小。
3. 不再使用固定的“10 个面一组”模板，也不再只保留分数最高的前 60 个候选；这样不会漏掉机身前后段的窗户。
4. 3/7/8 边的大型异形内环通常是舱门或检修口，不纳入窗户候选。
5. 使用 `view_window_candidates.py --all --highlight-host` 一次性检查全机候选；确认全部正确后，再批量写入 `window` 实例。

## 明天

处理 **245 A-10**：先检查原生贴花；如存在窗户，再按当前自动候选流程核对并标注。
