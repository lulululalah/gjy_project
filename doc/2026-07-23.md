# 2026-07-23：重注入数据的贴花重做

## 范围

仅对 [2026-07-19](2026-07-19.md) 中明确需要新增贴花的模型执行贴花注入；已有窗户、尾翼图案或其他可视几何特征的模型不新增贴花，但需将当前 `new_data` 结果正常同步至 `after_two`。

当前流程固定为：`data/plane_model/new_data`（输入）→ `data/plane_model/after_two`（贴花阶段输出）。不使用或创建 `after_rivet`。

重注入会改变面编号，因此 07-19 中的宿主面仅作历史参考；每个模型都必须在当前 `new_data` 产物上重新选面、注入并人工验收。

## 待处理清单

| 顺序 | 模型 | 贴花 | 本次宿主面 | 当前输入状态 | 本次状态 |
|---:|---|---|---|---|---|
| 1 | `109-ww1-standart-e-1-aircraft CATIA STP_standart-e1_wing_rivets.step` | 五角星 | F78 | `after_two` 已生成 9 个标签实例（8 铆钉、1 个 F78 贴花） | 已人工检查通过 |
| 2 | `221-cessna-model-grand-caravan STP_cessn CARAVAN v13_wing_rivets.step` | `V13` | F3772 | `after_two` 已生成 9 个标签实例（8 铆钉、1 个 F3772 贴花） | 已人工检查通过 |
| 3 | `237-an-124 STP STL_Antonov An-124 Rusian v2_wing_rivets.step` | `V2`（缩放 0.19） | F207 | `after_two` 已生成 17 个标签实例（16 铆钉、1 个 F207 贴花）；零厚度字形在 B 样条曲面上存在扭曲 | 按当前结果保留 |
| 4 | `47- a340-300 stp_a340 v3_wing_rivets.step` | `V3`，面内旋转 180° | F230 | `after_two` 已生成 9 个标签实例（8 铆钉、1 个 F230 贴花） | 已人工检查通过 |

## 无需新增贴花的同步处理

| 模型 | 结论 | 处理结果 |
|---|---|---|
| `116-TBF stp_TBM-1 Avenger v1_wing_rivets.step` | 已有贴花 | 已从 `new_data` 同步至 `after_two`，SHA-256 一致 |
| `144-P-80A Shooting Star STP STL_P-80A Shooting Star v1_wing_rivets.step` | 已有贴花 | 已从 `new_data` 同步至 `after_two`，SHA-256 一致 |
| `245-A-10 fairchild republic stp igs_A-10_wing_rivets.step` | 原有几何特征 | 已从 `new_data` 同步至 `after_two`，SHA-256 一致 |
| `27- DC-10 SolidWorks stp igs_DC 10_wing_rivets.step` | 窗户视为已有贴花 | 已从 `new_data` 同步至 `after_two`，SHA-256 一致 |
| `4-20 xi-an y20 STP STL_Xian Y-20 v3_wing_rivets.step` | 尾翼已有贴花 | 已从 `new_data` 同步至 `after_two`，SHA-256 一致 |
| `6--78 il-78 stp_IL-78 Fusion v19_wing_rivets.step` | 已有贴花 | 已从 `new_data` 同步至 `after_two`，SHA-256 一致 |
| `68- KC46 pegasus STP STL_Boeing KC-46 Peguses v4_wing_rivets.step` | 已有贴花 | 已从 `new_data` 同步至 `after_two`，SHA-256 一致 |
| `80- 777-300 SolidWorks stp igs_B777_wing_rivets.step` | 窗户视为已有贴花 | 已从 `new_data` 同步至 `after_two`，SHA-256 一致 |
| `87- 747-400 stp_beoing 747-400 v6_wing_rivets.step` | 窗户视为已有贴花 | 已从 `new_data` 同步至 `after_two`，SHA-256 一致 |
| `90- 737-800 stp_airplane boeing 737-800_wing_rivets.step` | 窗户视为已有贴花 | 已从 `new_data` 同步至 `after_two`，SHA-256 一致 |
| `91- 737-800 stp 3dm_737_wing_rivets.step` | 窗户视为已有贴花 | 已从 `new_data` 同步至 `after_two`，SHA-256 一致 |
| `95- 737-800-4 CREO STP_boeing_737_cj_wing_rivets.step` | 窗户视为已有贴花 | 已从 `new_data` 同步至 `after_two`，SHA-256 一致 |

## 阶段输出清理

- 已清空 `data/plane_model/after_two` 的全部 21 个文件（581,563,129 字节），目录本身保留。
- 后续仅将当前 `new_data` 的同步结果及本轮重新处理、验收通过的贴花结果写入 `after_two`。

## 单模型处理步骤

1. 在对应的 `new_data/*_wing_rivets.step` 上重新选择贴花宿主面。
2. 使用匹配的 `Detector.exe --inject-*-decal` 命令完成注入，输出写入 `after_two`。
3. 人工检查贴花外观与标签；通过后在本表记录最终宿主面、输出文件和验收结论。

```powershell
& "D:\Anaconda\envs\cad_graph_env\python.exe" ".\python\select_decal_face.py" ".\data\plane_model\new_data\<模型>_wing_rivets.step"
```

## 当前下一项

本轮新增贴花处理已完成：109 五角星、221 `V13`、237 `V2` 和 47 `V3` 均已写入 `after_two` 并完成处理结论记录。
