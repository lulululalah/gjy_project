# 2026-05-18 图/GNN 阶段记录

这是一份历史记录文档，用来说明项目切换到图方法并跑通首轮 GNN 训练时的状态。
当前主流程请以 [命令行使用说明](D:/Projects/gjy_byproject/doc/命令行使用说明.md) 和 [当前架构说明](D:/Projects/gjy_byproject/doc/当前架构说明.md) 为准。

## 当天完成的关键工作

1. 梳理了 `FeatureExtractor`、`Workflow`、`train_rivet_gcn.py`、`visualize_rivets.py` 的职责边界。
2. 将训练数据导出格式统一到按 `graph_id` 分组的图数据视角。
3. 重写 `python/train_rivet_gcn.py`，支持按模型构图、训练/验证拆分和模型权重保存。
4. 重写 `python/visualize_rivets.py`，支持调用 `Detector.exe --predict` 后直接做面级可视化。
5. 修正 `Workflow.cpp` 的文本输出与编码问题，保证导出链路可稳定运行。

## 当时确认的主链路

`STEP -> FaceFeature -> Face Graph -> RivetGNN -> Visualization`

当时的结论是：
- 项目重点已经从 STEP 几何修改转向图表示与识别。
- 每个 STEP 模型对应一个独立图，而不是把所有面混在一张大图里。
- 训练与推理都围绕 face-level 图特征展开。

## 当时跑通的流程

1. `python python\screen_dirty_models.py`
2. `.\build\Release\Detector.exe --train`
3. `python python\train_rivet_gcn.py`
4. `python python\visualize_rivets.py data\dirty_training_set\0.step`

## 历史意义

这份记录保留的原因是：
- 它标记了仓库从“数据导出准备阶段”进入“图/GNN 训练验证阶段”的时间点。
- 它解释了 `graph_id`、`rivet_gnn.pth`、`rivet_gnn_stats.npz` 这些现有文件命名的来源。

## 现在怎么看这份记录

可以把它当作一次阶段性里程碑，而不是当前操作手册。
如果后续流程与本文有冲突，以最新的架构文档和命令行文档为准。
