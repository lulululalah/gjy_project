# 2026-05-18 项目记录

## 今天做了什么

今天主要围绕“微小特征识别训练流程”做了整理、修正和跑通，没有进入 STEP 修改阶段。

完成的事情：

1. 阅读并梳理了项目现状
   - 重点阅读了 `doc/思路设计.txt`
   - 梳理了 `FeatureExtractor`、`Workflow`、`train_rivet_gcn.py`、`visualize_rivets.py` 的职责
   - 明确当前阶段重点是训练图神经网络识别微小特征，而不是修改 STEP

2. 移除了 `ModelGenerator`
   - 删除了 `include/ModelGenerator.h`
   - 删除了 `src/ModelGenerator.cpp`
   - 从 `CMakeLists.txt` 中移除了相关构建项

3. 重写了数据筛选脚本
   - 更新了 `python/screen_dirty_models.py`
   - 默认从 `C:\Users\1\Desktop\ABC` 扫描 STEP 数据
   - 默认把筛中的模型复制到 `data/dirty_training_set`
   - 输出筛选报告 `dirty_models_report.csv`

4. 重构了训练数据导出格式
   - 修改了 `src/Workflow.cpp`
   - 训练 CSV 现在包含：
     - `graph_id`
     - `model_name`
     - `id`
     - 以及各类几何/拓扑特征
   - 每个 STEP 模型现在对应一个独立图，而不是把所有模型拼成一张大图

5. 重构了训练脚本
   - 重写了 `python/train_rivet_gcn.py`
   - 改为按 `graph_id` 分组构图
   - 每个 STEP 对应一个 `torch_geometric.data.Data`
   - 加入了训练/验证集切分
   - 加入了 `DataLoader`
   - 保存模型权重 `rivet_gnn.pth`
   - 保存归一化统计 `rivet_gnn_stats.npz`

6. 重构了可视化推理脚本
   - 重写了 `python/visualize_rivets.py`
   - 支持命令行指定 STEP 文件
   - 支持自动调用 `Detector.exe --predict`
   - 使用 `rivet_gnn_stats.npz` 做同一套归一化
   - 加载 `rivet_gnn.pth` 后可对面级结果上色显示

7. 修复了 `Workflow.cpp` 的编码/编译问题
   - 之前中文输出乱码并导致编译报错
   - 将终端输出统一改成英文，避免编码问题

## 今天实际跑通的流程

### 1. 解决 Python 环境问题

确认可用环境：

- `cad_graph_env` 中可以导入 `OCC`
- 后续补装了 `pandas`
- 已确认以下模块可用：
  - `OCC`
  - `torch`
  - `torch_geometric`
  - `pandas`

### 2. 筛选训练模型

运行：

```powershell
python python\screen_dirty_models.py
```

结果：

- 从 `C:\Users\1\Desktop\ABC` 中筛出了 `29` 个候选模型
- 输出到 `data/dirty_training_set`
- 生成了 `dirty_models_report.csv`

### 3. 导出训练特征 CSV

重新编译后运行：

```powershell
.\build\Release\Detector.exe --train
```

确认导出格式正确：

```text
graph_id,model_name,id,area,relativeArea,perimeter,compactness,...
```

说明：

- 训练数据已经切换到新格式
- 现在 `full_training_set.csv` 可以按单个 STEP 构图训练

### 4. 开始训练图神经网络

运行：

```powershell
python python\train_rivet_gcn.py
```

训练日志：

```text
Loaded 29 graphs from data\full_training_set.csv
Train graphs: 23, Val graphs: 6
Training on: cpu
Epoch 001 | train_loss=1.1169 | val_loss=0.8934 | val_acc=0.6676
Epoch 010 | train_loss=0.5727 | val_loss=0.6696 | val_acc=0.6166
Epoch 020 | train_loss=0.4728 | val_loss=0.5882 | val_acc=0.7078
Epoch 030 | train_loss=0.4071 | val_loss=0.4796 | val_acc=0.7775
Epoch 040 | train_loss=0.4170 | val_loss=0.4432 | val_acc=0.8177
Epoch 050 | train_loss=0.3820 | val_loss=0.4900 | val_acc=0.7909
Epoch 060 | train_loss=0.3646 | val_loss=0.4893 | val_acc=0.7480
Epoch 070 | train_loss=0.3716 | val_loss=0.5126 | val_acc=0.7748
Epoch 080 | train_loss=0.3411 | val_loss=0.5536 | val_acc=0.7158
Epoch 090 | train_loss=0.3500 | val_loss=0.4976 | val_acc=0.7936
Epoch 100 | train_loss=0.3545 | val_loss=0.4881 | val_acc=0.8177
Epoch 110 | train_loss=0.2968 | val_loss=0.4777 | val_acc=0.7855
Epoch 120 | train_loss=0.2708 | val_loss=0.5259 | val_acc=0.7694
Epoch 130 | train_loss=0.2649 | val_loss=0.5623 | val_acc=0.7346
Epoch 140 | train_loss=0.3176 | val_loss=0.5771 | val_acc=0.7480
Epoch 150 | train_loss=0.2777 | val_loss=0.4713 | val_acc=0.7989
Best model saved to: rivet_gnn.pth
Feature stats saved to: rivet_gnn_stats.npz
```

当前判断：

- 训练已经正常开始
- `train_loss` 明显下降
- `val_loss` 在 40 轮附近达到较好水平，后面有波动但整体仍可接受
- `val_acc` 最高到 `0.8177`
- 模型确实学到了面级微小特征的区分能力

### 5. 第一次可视化推理验证

运行了可视化推理脚本，对筛选出的训练模型做了第一次面级预测显示。

使用方式示例：

```powershell
python python\visualize_rivets.py data\dirty_training_set\0.step
```

当前可视化脚本逻辑：

- 灰色：`label 0`
- 绿色：`label 1`
- 红色：`label 2`

第一次可视化结果现象：

- 模型在一个局部小结构附近标出了两个绿色小面
- 没有把大面积主体面错误标成目标特征
- 说明推理链路已经跑通，模型开始具备“关注局部小特征”的能力

当前判断：

- 结果方向是合理的
- 但还不能说明模型已经完全学对
- 下一步需要继续对多个模型做可视化检查，判断：
  - 是否存在漏检
  - 是否只识别出局部特征的一小部分面
  - 是否会对非目标小面产生误检

## 当前训练设置

`python/train_rivet_gcn.py` 当前默认训练轮数是：

```text
150 epochs
```

对应代码参数：

```python
parser.add_argument("--epochs", type=int, default=150, help="Training epochs. Default: 150")
```

也就是说：

- 默认总共训练 `150` 轮
- 目前看到 `Epoch 001`、`Epoch 010` 只是其中前几轮日志

如果想改训练轮数，可以这样运行：

```powershell
python python\train_rivet_gcn.py --epochs 50
```

## 今天遇到的问题

1. Python 环境不完整
   - 一开始 `ModuleNotFoundError: No module named 'OCC'`
   - 后来确认 `cad_graph_env` 有 `pythonocc`
   - 之后又补装了 `pandas`

2. `Workflow.cpp` 中文输出乱码
   - 控制台编码和源文件编码不一致
   - 一度导致字符串常量损坏、编译报错
   - 最后通过重写为干净 ASCII 版本解决

3. `full_training_set.csv` 一度看起来没更新
   - 原因是文件被表格软件占用
   - 关闭占用程序后重新导出，表头恢复正确

## 现在项目所处阶段

当前阶段已经不是“几何修改”，而是：

1. 从 STEP 模型提取面级几何/拓扑特征
2. 将每个 STEP 构造成一个图
3. 用图神经网络学习微小特征识别
4. 后续通过可视化检查模型识别结果是否合理

## 建议的下一步

1. 已完成一轮训练，模型和统计文件已生成
2. 已完成第一次可视化验证
3. 下一步继续选多个模型做可视化检查，例如：

```powershell
python python\visualize_rivets.py data\dirty_training_set\10.step
python python\visualize_rivets.py data\dirty_training_set\21.step
python python\visualize_rivets.py data\dirty_training_set\99.step
```

4. 继续观察模型是否把合理的局部面标成目标特征
5. 再决定是否调整：
   - `rivet_gnn.pth`
   - `rivet_gnn_stats.npz`
   - 标签规则
   - 节点特征
   - 卷积层结构
   - 训练轮数

## 当前停留进度

今天结束时，项目停留在：

1. 已完成数据筛选
2. 已完成新格式训练 CSV 导出
3. 已完成 150 轮训练
4. 已生成：
   - `rivet_gnn.pth`
   - `rivet_gnn_stats.npz`
5. 已完成第一次可视化推理
6. 明天的直接起点是：
   - 继续对多个模型做可视化检查
   - 判断模型是“部分识别正确”还是“标签语义本身需要调整”
