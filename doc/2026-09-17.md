# 2026-09-17 实验推进记录

## 今日完成

### E1：小几何检测主案例

- 固定了与 E2/E3 同源的检测工件：`rivet_gnn_xian20_train_simpletest_50ep.pth`、对应 `*_stats.npz` 和 `uv_test5_xian20_simpletest_cessna.csv`。
- 对 Cessna Citation 2、747-400、DC-10 从最终 STEP 端到端重新导出特征并推理；三架的 STEP 面号和 48 维特征均与真值 CSV 对齐。
- 主案例共 2,031 个 B-Rep 面：总体准确率 0.9818，宏平均 F1 为 0.9683，前景宏平均 F1 为 0.9584；rivet 的 Precision/Recall/F1 为 0.9074/1.0000/0.9515，surface_feature 为 0.9742/0.9569/0.9654。
- 完成逐飞机背景误检风险表：Cessna、747-400、DC-10 的背景误检总数分别为 4、14、2。
- 发现旧的 `rivet_gnn_xian20_train_simpletest_50ep_eval.csv` 总支持数为 3,631，而当前同名测试 CSV 约 3.4 万面；两者不是同一测试快照，不能在论文中混用。`Airplane body` 的当前 STEP（708 面）与旧真值（702 面）也不匹配。

### E2：去除与重建

- 已完成 9 架飞机的端到端去除与重建，结果位于 `work/test_removal_pipeline/` 和 `data/plane_model/ending/`。
- Cessna、747-400、DC-10 的简化 STEP 可重新读取，作为 E3 的稳定样本；其自由边分别为 `376 -> 147`、`3851 -> 127`、`8 -> 8`。

### E3：表面网格

- 固定 OpenCASCADE 表面网格参数：相对弦高 `0.0005`、角度偏差 `0.5 rad`，每组重复 5 次并报告中位耗时。
- 三角形数降幅：Cessna 3.35%，747-400 35.59%，DC-10 15.31%；网格耗时降幅：21.54%、45.84%、57.40%。
- 保留表面与原始 B-Rep 在简化到原始方向上的采样距离接近数值零；不将“原始到简化”距离解释为整体壳面误差，因为该方向包含有意删除的小几何。

## 明天优先做什么

1. **制作 E1 论文图（最高优先级）**：为 Cessna、747-400、DC-10 各导出一张带真值/预测/误检标记的截图。优先展示 747-400 的 10 个 background-to-rivet 误检，以及 Cessna 的 surface_feature 误检；图注必须写明类别和面数。
2. **整理 E1 结果表进论文**：采用 `results/e1_detection/主案例_Cessna_747_DC10/` 中的 JSON 和 CSV；不要引用旧 `*_eval.csv` 的 3,631 面指标。
3. **补齐 E3 图表版式**：将 `E3_表面网格规模与耗时.png` 与网格质量/几何差异表放入论文结果章节，明确其为“表面网格”而非 CFD 求解时间。
4. **决定扩展泛化实验是否重建**：若论文需要 9 架的全量 E1 表，先恢复或重建与检查点匹配的测试快照；之后一次性重新评估，不能在当前 3.4 万面 CSV 上套用旧汇总指标。

## 明日检查命令

```powershell
# 检查主案例指标
Get-Content 'D:\projects\gjy_byproject\results\e1_detection\主案例_Cessna_747_DC10\E1_检测指标汇总.json'

# 检查总汇总与 Git 工作区
git -C 'D:\projects\gjy_byproject' status --short
```

## 保留与清理原则

- 保留：`results/论文实验汇总.md`、E1 主案例汇总、E2 原始输出、E3 原始统计与图片、5 个复现脚本。
- 删除：全量 E1 诊断时生成的特征 CSV、非主案例预测 CSV、与当前测试快照不匹配的全量诊断汇总，以及 Python 缓存。这些文件均可由保留的脚本、STEP、模型和测试 CSV 再生成。
