# 2026-09-06 未完成事项

## 今日已完成修改

- Airbus 窗户真值按拓扑邻接补标：98 个窗户主体、710 个窗框小面，共 808 个 `window` 面，并同步 JSON 标签和正式测试 CSV。
- 对 Airbus 预测增加大面积机身误报后处理，抑制 3 个异常大机身面。
- 使用后处理结果重新统计测试集：总体准确率 89.51%，Airbus 准确率 90.99%。
- 清理明确废弃的一次性脚本、旧测试 CSV、旧候选清单、旧预测导出和旧截图；保留核心代码、数据、模型、STEP、标签及 `doc` 文件夹。

## 明日第一项工作

对所有测试数据的标签逐架进行可视化检查，确认是否存在漏标或误标。

检查重点：

1. 窗户主体和窗户周围的窗框小面是否全部标注；
2. 窗户内部面与外部面是否同时纳入真值；
3. 是否有机身大面、发动机或其他结构被误标为 `surface_feature`；
4. 发现问题后记录具体 `face_id`，再同步 JSON 标签和正式测试 CSV。

## 待办输出

- 为每个测试模型打开真值可视化窗口；
- 逐架复核并记录漏标/误标面；
- 完成修正后重新统计测试准确率。
