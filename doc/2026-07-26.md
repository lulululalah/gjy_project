# 2026-07-26

## 模型级 13 训 / 3 测划分

- 以 `data/wing_rivet_training_set.csv` 的 `model_name`（每架飞机一个 `graph_id`）为分组键，生成独立 CSV；同一飞机的面不会跨训练与验证。
- 训练集：13 架、24,530 个面。
- 验证集：3 架、8,633 个面：`95-737-800-4`、`68-KC-46`、`221-Cessna Caravan`。验证集包含 background、rivet、decal、window 四类。
- 输出：`data/plane_model/plane_model_face_train_13.csv` 与 `data/plane_model/plane_model_face_val_3.csv`；旧的 19/5 CSV 未覆盖。

生成命令：

```powershell
D:\Anaconda\envs\cad_graph_env\python.exe .\python\split_training_by_model.py
```

后续训练命令：

```powershell
D:\Anaconda\envs\cad_graph_env\python.exe .\python\train_rivet_gcn.py --csv data\plane_model\plane_model_face_train_13.csv --val-csv data\plane_model\plane_model_face_val_3.csv --training-mode window --window-hop 2 --epochs 150 --model-out rivet_gnn_no_centerz_split13_3.pth --stats-out rivet_gnn_no_centerz_split13_3_stats.npz --eval-out rivet_gnn_no_centerz_split13_3_eval.csv
```

## 95 窗户候选审计

- 新增 `python/audit_window_candidates.py`，只比较候选与既有真值，不修改标签、训练 CSV 或切分。
- 固定 4 边内环候选规则输出 82 个候选，均只包含真值窗户面。
- 将内环宿主面纳入候选组后，覆盖 410 / 428 个窗户面（95.79%）；82 个窗实例中 80 个完整覆盖、2 个部分覆盖、0 个完全漏失。
- 余下 18 个面集中在实例 32 与 33，均为一个实例内的多组窗口面；下一步补充该聚合规则后再做跨模型候选召回审计。

## 跨模型候选路径审计

- 95 的 4 边内环规则是 CREO 737 专用拓扑：在 DC-10、B777、747、737-800（90）、3dm 737 上均为 0 候选，不能作为统一规则。
- 747 的单面窗可由一个已确认种子经孤立重复面路径生成 210 个候选，覆盖 208 / 208 个真值窗面；其中仅 2 个候选不是窗。
- 3dm 737 的双面窗可由一个已确认面对生成 86 个候选，覆盖 172 / 172 个真值窗面，无非窗候选。
- 新增 `python/export_repeated_window_group_candidates.py`：从一个已确认的三面及以上窗模板，按面类型、边数、面积和相对中心位置生成重复组候选。
- B777 的四面模板生成 63 个纯窗候选，覆盖 252 / 520 个窗面；737-800（90）的八面模板存在更强几何变形，单模板只覆盖 15 / 368 个窗面。后续需从训练模型建立多个已确认模板的候选库并合并去重。
