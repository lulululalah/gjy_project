# 2026-07-25

## 原生特征标注

- 完成 B777（80）的客舱窗户标注：130 个实例、520 个面。
- 完成 747-400（87）的客舱窗户标注：208 个实例、208 个面；驾驶舱玻璃 F4、F5 未作为客舱窗户标注。
- 完成 737-800（90）的客舱窗户标注：46 个实例、368 个面。
- 91 的标签以最新铆钉标签为底重新合并：26 个铆钉面、26 个贴花面、172 个窗户面。
- 4-20 与 KC-46 的标签以 `new_data` 的最新铆钉面为底，重新写入已确认的原生贴花面，避免旧面号错位。

## 109 与最终数据集

- 109 Standard E-1 不在 15 架“重新注入铆钉”表中，但属于最终训练模型。
- 以 `new_data` 的最新铆钉 STEP 为输入，在已确认宿主面 F78 重建五角星贴花。
- 109 最终标签包含 26 个铆钉面和 1 个贴花面。
- 最终训练数据共 16 架模型；“15”仅指 07-19 记录中的铆钉重注入模型数。

## 数据整理

- `after_two` 仅保存最终 STEP；`label` 保存同名最终 JSON。
- 已删除全部窗户候选与历史 CSV，不作为训练真值使用。
- 已删除没有对应最终 STEP 的旧标签，以及 A-10 的重复原始 STEP。
- 当前 `after_two` 与 `label` 均为 16 份，一一对应，无缺失、无孤立标签。
- `new_data` 保留为最新铆钉阶段的输入/中间数据；训练使用 `after_two` 的最终 STEP 与 `label` 的对应 JSON。

## 训练导出

- 修改 `src/Workflow.cpp`：训练导出器从 `data/plane_model/after_two` 读取最终 STEP，从 `data/plane_model/label` 读取同名 JSON；支持普通 `*_wing_rivets.step` 和 `*_wing_rivets_decals.step`。
- 标签解析器兼容格式化（多行）JSON。
- 已重新编译 Debug 版 `Detector.exe`。

导出命令：

```powershell
.\build\Debug\Detector.exe --export-wing-rivet-training ".\data\plane_model"
```

导出结果：

- 成功处理 16 架模型，跳过 0 架。
- `data/wing_rivet_training_set.csv` 共 33163 个面样本。
- 标签数量：背景 30622、铆钉 666、贴花 141、窗户 1734。

## 下一步

- 以模型为单位生成 13 训 / 3 测划分，避免同一飞机的面同时出现在训练和测试中。
- 后续新增的 10 架仅贴花模型不加入训练，作为额外贴花测试集；真值仅包含 `background` 与 `decal`。
