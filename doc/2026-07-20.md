# 2026-07-20：重注入收尾与贴花流程迁移

## 今日完成

- `95- 737-800-4 CREO STP_boeing_737_cj`：宿主面 F2711，8 个铆钉；已人工检查通过，面编号一致性检查通过。
- `144-P-80A Shooting Star`：重新尝试注入。默认策略未找到合格接缝后，人工选择相对宿主面 F212、F218；每个面沿两条有效边界接缝生成 8 个铆钉，共 16 个。标签实例分布为 F212：8、F218：8；面编号一致性检查通过，已人工检查通过。
- 747-400 v7：已删除 `source` 中的原始 STEP 与旧 `after_rivet` 中的仅铆钉 STEP；此前已清理训练 CSV、标签与训练 STEP。v6 保留，避免同族 train/val 数据泄漏。

## 目录与代码调整

- 已删除 `data/plane_model/after_rivet`，共 48 个旧派生文件（约 581 MB）。
- 铆钉重注入的有效流程保持为：`data/plane_model/source` → `data/plane_model/new_data`。
- 贴花功能保留，但其输入入口已从 `after_rivet` 改为 `new_data`：
  - `Detector.exe --inject-star-decals`
  - `Detector.exe --inject-v13-decal`
  - `Detector.exe --inject-v2-decal`
  - `Detector.exe --inject-v3-decal`
- 贴花命令会从 `new_data` 中读取 `*_wing_rivets.step` 及同名 `.labels.json`，输出仍写入 `data/plane_model/after_two`。
- `python/select_decal_face.py` 的输入说明与相关操作文档已同步为 `new_data`。
- 已重新编译 `Detector`，通过。
·
## 明日贴花重做

1. 144 的 F212/F218 注入结果已人工检查通过，纳入贴花重做范围。
2. 对每个已确认的 `new_data/*_wing_rivets.step`，在 `new_data` 中选择贴花宿主面：

   ```powershell
   & "D:\Anaconda\envs\cad_graph_env\python.exe" ".\python\select_decal_face.py" ".\data\plane_model\new_data\<模型>_wing_rivets.step"
   ```

3. 使用选定面重新注入贴花；例如星形贴花：

   ```powershell
   .\build\Debug\Detector.exe --inject-star-decals ".\data\plane_model\new_data\<模型>_wing_rivets.step" --host-face <面ID>
   ```

4. 人工检查 `after_two` 中的贴花结果与标签，再更新贴花选择记录。

## 注意

- 不再使用或创建 `after_rivet`。
- `new_data` 是明日贴花重做的唯一输入来源；`after_two` 仅保存贴花阶段输出。
- 90 号 737 虽暂按通过，仍需在贴花前重点复查外观与宿主面选择。
